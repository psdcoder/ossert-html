html {
  box-sizing: border-box;
}

*,
*::before,
*::after {
  box-sizing: inherit;
}

[data-balloon]::after {
  padding: 5px 10px;
  font-size: 12px;
  line-height: 18px;
  background-color: rgba(0, 0, 0, 0.65);
}
