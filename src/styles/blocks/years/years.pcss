.years {
  margin: 0;
  padding: 0;
  list-style: none;

  &__item_active > .years__link {
    text-decoration: none;
    color: $textColorSecondary;
    cursor: default;
    pointer-events: none;

    &::after {
      content: '\25B6';
      position: absolute;
      top: 1px;
      display: inline-block;
      margin-left: 5px;
      font-size: 14px;
      line-height: calc(18px * 1.5);
    }
  }

  &__link {
    position: relative;
    padding: 2px 10px;
    font-size: 18px;
    line-height: calc(18px * 1.5);
  }
}
