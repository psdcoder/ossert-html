.footer {
  display: flex;
  justify-content: center;
  align-items: center;
  margin-top: calc($layoutFactor * 2);
  padding-top: $layoutFactor;
  padding-bottom: $layoutFactor;
  font-family: $fontFamily;
  font-size: $layoutFactor;
  line-height: $layoutFactor;
  background-color: $themePrimary;
  color: #fff;
}
